# 云南自然灾害应急协同决策平台 - 结项文档

## 1. 项目概述

### 1.1 项目基本信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 云南自然灾害应急协同决策平台 |
| 项目类型 | 多智能体驱动的应急响应系统 |
| 技术架构 | 前后端分离 + AI服务 + 向量数据库 |
| 开发周期 | 2026年7月 |
| 项目状态 | ✅ 已完成（含地图态势展示全链路打通） |

### 1.2 项目目标

本项目旨在建设一套面向云南省自然灾害应急响应的协同决策平台，实现：

- **灾情快速上报**：支持图文混合上报，实现灾情信息的快速录入和分发
- **智能方案生成**：基于大语言模型和应急预案知识库（RAG），自动生成应急处置方案
- **资源高效调度**：实现应急资源的统一管理、锁定/释放和智能调度
- **决策可视化**：通过数据大屏展示灾害态势、资源分布和处置进度
- **多角色协同**：支持 VIEWER、OPERATOR、RESOURCE_MANAGER、ADMIN 四种角色协同工作
- **地理编码服务**：灾情上报时根据地址自动调用高德API获取经纬度，支持本地城市坐标缓存和降级匹配
- **地图态势展示**：基于Leaflet在地图上展示灾情点位，支持聚合和筛选
- **历史数据坐标补全**：提供批量补全接口，为历史灾情数据补充经纬度

---

## 2. 技术栈

### 2.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4+ | 前端框架 |
| TypeScript | 5.4+ | 类型安全 |
| Vite | 5.2+ | 构建工具 |
| Element Plus | 2.6+ | UI组件库 |
| Pinia | 2.1+ | 状态管理 |
| Vue Router | 4.3+ | 路由管理 |
| Axios | 1.6+ | HTTP客户端 |
| ECharts | 5.5+ | 图表可视化 |

### 2.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 21 | 编程语言 |
| Spring Boot | 3.2+ | 后端框架 |
| Spring Security | 6.2+ | 安全框架 |
| Spring Data JPA | 3.2+ | ORM框架 |
| JWT | - | 身份认证 |
| Redis | 7.0+ | 缓存和分布式锁 |
| MinIO | - | 文件存储 |
| Lombok | 1.18+ | 代码简化 |

### 2.3 AI服务技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.10 | 编程语言 |
| FastAPI | 0.140+ | API框架 |
| LangGraph | 1.2+ | 多Agent工作流编排 |
| Transformers | 5.14+ | 模型库 |
| sentence-transformers | 5.6+ | Embedding模型 |

### 2.4 LLM与向量技术

| 技术 | 版本 | 用途 |
|------|------|------|
| vLLM | 0.4+ | LLM推理引擎 |
| Qwen2.5-7B-Instruct | - | 中文大语言模型 |
| BAAI/bge-small-zh-v1.5 | - | 中文Embedding模型（512维） |
| pgvector | - | PostgreSQL向量扩展 |

### 2.5 数据库技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| MySQL | 8.0+ | 主业务数据库 |
| PostgreSQL | 15+ | 向量数据库（含pgvector） |
| Redis | 7.0+ | 缓存和分布式锁 |

---

## 3. 系统架构

### 3.1 总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端层 (Vue3)                           │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐        │
│  │ 灾情   │ │ 方案   │ │ 资源   │ │ 调度   │ │ 大屏   │        │
│  │ 管理   │ │ 管理   │ │ 管理   │ │ 指令   │ │ 展示   │        │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘        │
│                        ↓ Axios + Pinia ↓                        │
├─────────────────────────────────────────────────────────────────┤
│                       网关层 (Vite/Nginx)                        │
│                    反向代理 · 路由转发 · CORS                    │
├─────────────────────────────────────────────────────────────────┤
│                       后端层 (Spring Boot)                       │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  JWT认证 · RBAC权限 · 审计日志(AOP) · 分布式锁(Redis)  │    │
│  └─────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────┤
│                      AI服务层 (FastAPI)                         │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                    │
│  │ 情报   │ │ RAG检索│ │ 资源   │ │ 方案   │                    │
│  │ 分析   │ │ 向量   │ │ 调度   │ │ 审查   │                    │
│  │ Agent  │ │ 检索   │ │ Agent  │ │ Agent  │                    │
│  └────────┘ └────────┘ └────────┘ └────────┘                    │
├─────────────────────────────────────────────────────────────────┤
│                        LLM层 (vLLM)                             │
│              Qwen2.5-7B-Instruct 中文推理服务                   │
├─────────────────────────────────────────────────────────────────┤
│                        数据层                                   │
│  ┌────────┐ ┌────────────┐ ┌────────┐ ┌────────┐                │
│  │ MySQL  │ │ PostgreSQL │ │ Redis  │ │ MinIO  │                │
│  │ 业务   │ │ 向量       │ │ 缓存   │ │ 文件   │                │
│  │ 数据   │ │ 数据       │ │ 锁     │ │ 存储   │                │
│  └────────┘ └────────────┘ └────────┘ └────────┘                │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 核心业务流程

#### 3.2.1 灾情上报流程

```
用户(VIEWER) → 前端 → POST /api/incident/report
                         ↓
                    后端(Spring Boot) → 自动地理编码(高德API + 本地缓存 + 降级匹配)
                         ↓
                    上传图片到MinIO
                         ↓
                    MySQL → INSERT INTO incidents (含latitude/longitude)
                         ↓
                    返回 incidentId + imageUrls
```

#### 3.2.2 AI方案生成流程

```
用户(OPERATOR) → 前端 → POST /api/plan/generate
                          ↓
                     后端(Spring Boot) → 查询灾情信息
                          ↓
                     AI服务(FastAPI) → 情报分析Agent → RAG检索Agent
                          ↓                                        ↓
                     方案生成Agent ← 资源调度Agent ← pgvector    MySQL
                          ↓
                     方案审查Agent（最多5次重试）
                          ↓
                     返回 planId + citations
                          ↓
                     后端保存方案到MySQL
```

#### 3.2.3 资源调度流程

```
用户(RESOURCE_MANAGER) → 前端 → POST /api/resource/dispatch
                                    ↓
                               后端 → Redis分布式锁
                                    ↓
                               检查库存 → 更新库存 → 记录调度
                                    ↓
                               释放锁 → 返回结果
```

#### 3.2.4 处置结束流程

```
用户(RESOURCE_MANAGER/ADMIN) → 前端 → 点击"处置结束"按钮
                                        ↓
                                   确认对话框
                                        ↓
                                   POST /api/incident/complete
                                        ↓
                                   后端验证：处置方案已接受
                                        ↓
                                   更新 incident.status = 'completed'
                                   更新 incident.resourceDispatchStatus = 'completed'
                                        ↓
                                   返回已结束的灾情信息
```

---

## 4. 功能模块

### 4.1 认证模块

| 功能 | 描述 | 角色 |
|------|------|------|
| 用户登录 | 用户名+密码登录，获取JWT Token | 所有角色 |
| 用户注册 | 新用户注册，默认分配VIEWER角色 | 新用户 |
| 角色申请 | VIEWER申请升级为OPERATOR或RESOURCE_MANAGER | VIEWER |
| 角色审核 | ADMIN审核角色申请 | ADMIN |

### 4.2 灾情管理模块

| 功能 | 描述 | 角色 |
|------|------|------|
| 灾情上报 | 图文混合上报，支持图片上传，自动地理编码获取经纬度 | 所有角色 |
| 灾情列表 | 分页列表，支持多条件筛选 | 所有角色 |
| 灾情详情 | 查看灾情详情、图片、处理记录、经纬度信息 | 所有角色 |
| 历史数据坐标补全 | 批量为历史灾情数据补充经纬度（仅限ADMIN） | ADMIN |
| 处置结束 | 标记事件为"已结束" | RESOURCE_MANAGER, ADMIN |

### 4.3 方案管理模块

| 功能 | 描述 | 角色 |
|------|------|------|
| 方案生成 | AI自动生成应急处置方案 | OPERATOR, ADMIN |
| 方案列表 | 查看灾情相关方案 | 所有角色 |
| 方案详情 | 查看方案详细内容 | 所有角色 |
| 方案流式输出 | SSE打字机效果输出 | OPERATOR, ADMIN |
| 方案审核 | 通过/驳回/修改方案 | OPERATOR, ADMIN |

### 4.4 资源管理模块

| 功能 | 描述 | 角色 |
|------|------|------|
| 资源列表 | 查看应急资源，支持类型筛选 | RESOURCE_MANAGER, ADMIN |
| 资源调度 | 锁定/释放资源，Redis分布式锁 | RESOURCE_MANAGER, ADMIN |
| 资源申请 | OPERATOR提交资源申请 | OPERATOR |
| 调度指令 | 管理调度指令，处理资源申请 | RESOURCE_MANAGER, ADMIN |
| 驳回申请 | 驳回资源申请并填写理由 | RESOURCE_MANAGER, ADMIN |

### 4.5 系统管理模块

| 功能 | 描述 | 角色 |
|------|------|------|
| 用户管理 | 创建/修改/禁用用户 | ADMIN |
| 知识库管理 | 上传/删除应急预案文档 | ADMIN |
| 审计日志 | 查看系统操作日志 | ADMIN |
| Agent审计 | 查看AI Agent执行记录 | ADMIN |

### 4.6 Dashboard模块

| 功能 | 描述 | 角色 |
|------|------|------|
| 概览统计 | 今日数、活跃数、待处理数 | 所有角色 |
| 趋势分析 | 灾情数量时间趋势图 | 所有角色 |
| 分布统计 | 类型/地区分布图 | 所有角色 |
| 数据大屏 | 全局指挥中心大屏 | 所有角色 |
| 地图态势展示 | Leaflet地图展示灾情点位，支持按类型/状态筛选 | 所有角色 |

---

## 5. 数据库设计

### 5.1 数据库概览

| 数据库 | 类型 | 用途 |
|--------|------|------|
| emergency_db | MySQL 8.0+ | 主业务数据库 |
| emergency_vector | PostgreSQL 15+ + pgvector | 向量数据库 |

### 5.2 MySQL数据表（实际使用）

| 表名 | 实体类 | 说明 | 状态 |
|------|--------|------|------|
| users | User.java | 用户表 | ✅ 已实现 |
| roles | Role.java | 角色表 | ✅ 已实现 |
| incidents | Incident.java | 灾情表（含latitude/longitude经纬度字段） | ✅ 已实现 |
| plans | Plan.java | 方案表（AI方案+处置方案） | ✅ 已实现 |
| citations | Citation.java | 引用表 | ✅ 已实现 |
| emergency_resource | EmergencyResource.java | 应急物资表 | ✅ 已实现 |
| resource_dispatch_record | ResourceDispatchRecord.java | 物资调度记录表 | ✅ 已实现 |
| resource_requests | ResourceRequest.java | 资源申请表 | ✅ 已实现 |
| audit_logs | AuditLog.java | 审计日志表 | ✅ 已实现 |
| agent_runs | AgentRun.java | Agent执行记录表 | ✅ 已实现 |
| knowledge_files | Knowledge.java | 知识库文件表 | ✅ 已实现 |

### 5.3 PostgreSQL数据表

| 表名 | 实体类 | 说明 | 状态 |
|------|--------|------|------|
| knowledge_chunks | EmergencyPlan.java | 知识片段表（含512维向量） | ✅ 已实现 |
| locations | Location.java | 地理位置表 | ✅ 已实现 |
| resources | Resource.java | PostgreSQL资源表 | ✅ 已实现 |

### 5.4 文档中定义但未实现的冗余表

以下表在设计文档中有定义，但实际代码中未实现，功能已由其他表承担：

| 文档表名 | 原始设计用途 | 冗余原因 | 处理建议 |
|----------|-------------|---------|---------|
| disposal_plans | 存储人工提交的处置方案 | 功能已由 `plans` 表承担，通过 status 字段区分（submitted/accepted/rejected） | 🗑️ 可从文档中删除 |
| resource_shortage | 存储资源不足警告 | 功能已集成在资源调度流程中，通过 incident.resourceDispatchStatus 字段标记 | 🗑️ 可从文档中删除 |
| role_applications | 存储角色升级申请 | 功能未实现，相关接口未开发 | 🗑️ 可从文档中删除 |

### 5.5 表关系图

```
users ──1:N──> incidents (reporter_id)
users ──1:N──> plans (生成方案)
users ──1:N──> resource_requests (申请资源)
users ──1:N──> audit_logs (操作日志)
users ──1:N──> agent_runs (触发Agent)
users ──N:1──> roles (role_id)

incidents ──1:N──> plans (incident_id)
incidents ──1:N──> resource_requests (incident_id)
incidents ──1:N──> resource_dispatch_record (incident_id)
incidents (含latitude/longitude经纬度字段)

plans ──1:N──> citations (plan_id)

emergency_resource ──1:N──> resource_dispatch_record (resource_id)
emergency_resource ──1:N──> resource_requests (resource_id)
```

---

## 6. 角色权限体系

### 6.1 角色定义

| 角色 | 名称 | 核心职责 |
|------|------|----------|
| VIEWER | 普通信息员 | 灾情上报、查看信息、申请角色升级 |
| OPERATOR | 应急指挥人员 | 生成方案、提交处置方案、灾情状态管理 |
| RESOURCE_MANAGER | 资源管理员 | 资源管理、资源调度、调度指令管理 |
| ADMIN | 系统管理员 | 系统配置、用户管理、审计日志、权限审核 |

### 6.2 权限矩阵

| 功能模块 | VIEWER | OPERATOR | RESOURCE_MANAGER | ADMIN |
|----------|:------:|:--------:|:----------------:|:-----:|
| 登录/注册 | ✅ | ✅ | ✅ | ✅ |
| 灾情上报 | ✅ | ✅ | ✅ | ✅ |
| 灾情列表/详情 | ✅ | ✅ | ✅ | ✅ |
| 方案生成 | ❌ | ✅ | ❌ | ✅ |
| 方案审核 | ❌ | ✅ | ❌ | ✅ |
| 方案列表/详情 | ✅ | ✅ | ✅ | ✅ |
| 资源列表 | ❌ | ❌ | ✅ | ✅ |
| 资源调度 | ❌ | ❌ | ✅ | ✅ |
| 资源申请 | ❌ | ✅ | ❌ | ❌ |
| 调度指令 | ❌ | ❌ | ✅ | ✅ |
| 驳回方案 | ❌ | ❌ | ✅ | ✅ |
| 处置结束 | ❌ | ❌ | ✅ | ✅ |
| 用户管理 | ❌ | ❌ | ❌ | ✅ |
| 知识库管理 | ❌ | ❌ | ❌ | ✅ |
| 审计日志 | ❌ | ❌ | ❌ | ✅ |
| Agent审计 | ❌ | ❌ | ❌ | ✅ |
| Dashboard | ✅ | ✅ | ✅ | ✅ |

---

## 7. 状态流转体系

### 7.1 灾情状态

```
pending（待确认）──→ confirmed（已确认）──→ processing（处置中）──→ completed（已结束）
    │
    └──→ 保持状态（可在pending/confirmed/processing间流转）
    
说明：
- pending：灾情上报后的初始状态，表示等待确认
- confirmed：灾情已被指挥人员确认
- processing：灾情正在处置中
- completed：处置结束，需满足处置方案已接受 + 资源调度已完成
```

### 7.2 处置方案状态（plans表）

```
submitted（已提交）──→ accepted（已接受）
        │                      │
        │                      └──→ 可调度资源 → 处置结束
        │
        └──→ rejected（已驳回）──→ OPERATOR重新拟定方案
```

### 7.3 资源调度状态（incident字段）

```
idle（空闲）──→ dispatching（调度中）──→ executing（执行中）──→ completed（已完成）
                                                       │
                                                       └──→ shortage（资源不足）
                                                            │
                                                            └──→ RESOURCE_MANAGER重新调度
```

### 7.4 资源申请状态（resource_requests表）

```
pending（待处理）──→ approved（已批准）
        │
        └──→ rejected（已驳回）──→ 显示驳回理由，OPERATOR重新拟定方案
```

---

## 8. API接口清单

### 8.1 认证接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/auth/login | 用户登录 |
| POST | /api/auth/register | 用户注册 |

### 8.2 灾情管理接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/incident/report | 灾情上报（multipart/form-data，支持自动地理编码） |
| GET | /api/incident/list | 灾情列表（分页+筛选，含经纬度字段） |
| GET | /api/incident/detail | 灾情详情（含经纬度字段） |
| POST | /api/incident/submit | 提交灾情 |
| POST | /api/incident/update-status | 更新灾情状态（pending/confirmed/processing/completed） |
| POST | /api/incident/complete | 处置结束 |
| POST | /api/incident/backfill-coordinates | 历史数据坐标补全（仅限ADMIN） |

### 8.3 方案管理接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/plan/generate | AI生成方案 |
| GET | /api/plan/list | 方案列表 |
| GET | /api/plan/detail | 方案详情 |
| GET | /api/plan/stream | 方案流式输出（SSE） |
| POST | /api/plan/review | 方案审核 |

### 8.4 处置方案接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/disposal-plan/submit | 提交处置方案 |
| GET | /api/disposal-plan/list | 处置方案列表 |
| POST | /api/disposal-plan/reject | 驳回处置方案 |

### 8.5 资源管理接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/resource/list | 资源列表 |
| POST | /api/resource/dispatch | 资源调度（支持多资源） |
| POST | /api/resource/{id}/lock | 锁定资源 |
| POST | /api/resource/{id}/release | 释放资源 |

### 8.6 资源申请接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | /api/resource-request | 提交资源申请 |
| GET | /api/resource-request/list | 申请列表 |
| GET | /api/resource-request/pending | 待处理申请 |
| POST | /api/resource-request/{id}/approve | 批准申请 |
| POST | /api/resource-request/{id}/reject | 驳回申请 |

### 8.7 Dashboard接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/dashboard/overview | 概览统计 |
| GET | /api/dashboard/trend | 趋势分析 |
| GET | /api/dashboard/distribution | 分布统计 |
| GET | /api/dashboard/screen | 大屏数据 |
| GET | /api/dashboard/map-data | 地图数据（含经纬度、灾害类型、状态） |

### 8.8 系统管理接口

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | /api/user/list | 用户列表 |
| POST | /api/user/create | 创建用户 |
| PUT | /api/user/update | 更新用户 |
| DELETE | /api/user/delete | 删除用户 |
| GET | /api/knowledge/list | 知识库列表 |
| POST | /api/knowledge/upload | 上传文档 |
| DELETE | /api/knowledge/delete | 删除文档 |
| GET | /api/audit/logs | 审计日志 |
| GET | /api/agent/runs | Agent执行记录 |

---

## 9. 前端路由

| 路径 | 页面 | 说明 |
|------|------|------|
| /login | 登录页 | 用户名密码登录 |
| /home | 首页仪表盘 | 统计概览、趋势、分布 |
| /incident/list | 灾情事件列表 | 分页表格、多条件筛选 |
| /incident/report | 灾情上报 | 图文混合上报表单 |
| /incident/:id | 事件详情 | 详情+方案+资源+调度 |
| /resource | 资源管理 | 资源列表、调度、锁定/释放 |
| /plan | 方案管理 | AI方案、处置方案列表 |
| /members | 用户管理 | ADMIN专属 |
| /audit | 审计日志 | ADMIN专属 |
| /screen | 数据大屏 | 指挥中心大屏展示 |

---

## 10. 枚举值对照

### 10.1 灾害类型

| 值 | 中文标签 |
|----|----------|
| earthquake | 地震 |
| mudslide | 泥石流 |
| flood | 洪涝 |
| drought | 干旱 |
| landslide | 山体滑坡 |
| fire | 森林火灾 |
| other | 其他 |

### 10.2 灾情级别

| 值 | 中文标签 |
|----|----------|
| I | Ⅰ级（特别重大） |
| II | Ⅱ级（重大） |
| III | Ⅲ级（较大） |
| IV | Ⅳ级（一般） |

### 10.3 灾情状态

| 值 | 中文标签 | 说明 |
|----|----------|------|
| pending | 待确认 | 灾情上报后的初始状态 |
| confirmed | 已确认 | 灾情已被指挥人员确认 |
| processing | 处置中 | 灾情正在处置中 |
| completed | 已结束 | 处置结束 |

### 10.4 处置方案状态

| 值 | 中文标签 |
|----|----------|
| draft | 草稿 |
| submitted | 待审核 |
| accepted | 已通过 |
| rejected | 已驳回 |

### 10.5 资源调度状态

| 值 | 中文标签 |
|----|----------|
| idle | 空闲 |
| executing | 执行中 |
| shortage | 资源不足 |
| completed | 已完成 |

### 10.6 资源类型

| 值 | 中文标签 |
|----|----------|
| team | 救援队伍 |
| medical | 医疗物资 |
| vehicle | 运输车辆 |
| shelter | 临时安置点 |

---

## 11. 部署说明

### 11.1 环境要求

| 服务 | 最低配置 | 推荐配置 |
|------|---------|---------|
| 前端 | 2核CPU, 4GB内存 | 2核CPU, 4GB内存 |
| 后端 | 4核CPU, 8GB内存 | 4核CPU, 8GB内存 |
| AI服务 | 8核CPU, 16GB内存 | 8核CPU, 16GB内存 |
| vLLM | 8核CPU, 32GB+GPU | 8核CPU, 32GB+GPU |
| MySQL | 4核CPU, 16GB内存 | 4核CPU, 16GB内存 |
| PostgreSQL | 4核CPU, 16GB内存 | 4核CPU, 16GB内存 |
| Redis | 2核CPU, 4GB内存 | 2核CPU, 4GB内存 |

### 11.2 端口配置

| 服务 | 端口 | 协议 |
|------|------|------|
| 前端（Vite开发） | 5173 | HTTP |
| 前端（生产Nginx） | 80 | HTTP |
| 后端（Spring Boot） | 8080 | HTTP |
| AI服务（FastAPI） | 8002 | HTTP |
| vLLM | 8000 | HTTP |
| MySQL | 3306 | TCP |
| PostgreSQL | 5432 | TCP |
| Redis | 6379 | TCP |

### 11.3 启动步骤

```bash
# 1. 启动数据库服务
# MySQL、PostgreSQL、Redis 需预先启动

# 2. 启动后端
cd backend
mvn spring-boot:run

# 3. 启动AI服务
cd ai-service
python main.py

# 4. 启动前端
cd frontend
npm install
npm run dev

# 5. 访问系统
# 浏览器打开 http://localhost:5173
```

### 11.4 默认账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | 123456 | ADMIN |
| operator | 123456 | OPERATOR |
| resource | 123456 | RESOURCE_MANAGER |
| viewer | 123456 | VIEWER |

---

## 12. 项目文件结构

```
Yunnan-Emergency-Response/
├── frontend/                          # 前端项目
│   ├── src/
│   │   ├── api/                       # API接口封装
│   │   ├── stores/                    # Pinia状态管理
│   │   ├── views/                     # 页面组件
│   │   │   ├── Incident/             # 灾情管理页面
│   │   │   ├── Plan/                 # 方案管理页面
│   │   │   ├── Resource/             # 资源管理页面
│   │   │   ├── ResourceRequest/       # 资源申请页面
│   │   │   ├── Home/                 # 首页仪表盘
│   │   │   ├── Login/                 # 登录页
│   │   │   ├── Member/               # 用户管理
│   │   │   ├── AuditLog/              # 审计日志
│   │   │   └── Screen/               # 数据大屏
│   │   ├── router/                    # 路由配置
│   │   ├── types/                     # TypeScript类型定义
│   │   ├── utils/                     # 工具函数
│   │   └── mock/                      # Mock数据
│   └── package.json
│
├── backend/                           # 后端项目
│   ├── src/main/java/com/project/
│   │   ├── controller/                # REST API控制器
│   │   ├── service/                    # 业务逻辑层
│   │   ├── repository/                # 数据访问层
│   │   ├── entity/                     # JPA实体
│   │   │   ├── mysql/                 # MySQL实体
│   │   │   └── postgres/              # PostgreSQL实体
│   │   ├── dto/                       # 数据传输对象
│   │   ├── config/                    # 配置类
│   │   ├── security/                  # 安全配置
│   │   ├── aspect/                    # AOP切面（审计日志）
│   │   └── common/                    # 通用类
│   └── pom.xml
│
├── 结训文档/                           # 结训文档
│   ├── 结项文档.md                    # 本文档
│   ├── 需求规格说明书.md
│   ├── 系统设计说明书.md
│   ├── 数据库设计.md
│   ├── 前后端接口文档.md
│   └── 测试报告.md
│
└── docs/                              # 技术文档
    ├── db-schema.md
    ├── api-docs.md
    └── performance_metrics.md
```

---

## 13. 数据统计

### 13.1 模拟数据

| 数据类型 | 数量 |
|----------|------|
| 用户 | 4条（admin/operator/resource/viewer） |
| 灾情事件 | 58条（其中55条已通过地理编码获取有效坐标） |
| 应急资源 | 20条 |
| 应急预案 | 6条（含AI方案和处置方案） |
| 知识片段（PostgreSQL） | 201条（512维向量） |

### 13.2 枚举映射

#### 灾情状态映射

| 原始值 | 前端标签 | 样式类型 |
|--------|---------|---------|
| processing | 处置中 | warning |
| completed | 已结束 | success |

#### 处置方案状态映射

| 原始值 | 前端标签 | 样式类型 |
|--------|---------|---------|
| draft | 草稿 | info |
| submitted | 待审核 | warning |
| accepted | 已通过 | success |
| rejected | 已驳回 | danger |

---

## 14. 已修复的问题清单

### 14.1 多资源调度问题

- **问题**：提交多个资源调度时失败
- **原因**：后端在第一个资源锁定后立即将灾情标记为 `completed`
- **修复**：改为标记为 `resourceDispatchStatus = 'executing'`，支持多次调度

### 14.2 状态显示不一致

- **问题**：新上报事件显示"pending"，历史方案显示原始状态值
- **修复**：
  - 新事件默认状态改为 `processing`（处置中）
  - 前端状态映射统一，`pending` 映射为"处置中"
  - 方案列表使用处置方案状态映射（DisposalPlanStatus）

### 14.3 资源调度驳回链路

- **问题**：管理员驳回资源调度后，指挥人员看不到驳回理由
- **修复**：
  - 驳回时同步更新 `disposalPlanStatus = 'rejected'`
  - 驳回时同步更新 `resourceDispatchStatus = 'shortage'`
  - OPERATOR可看到驳回理由并重新拟定方案

### 14.4 处置结束功能

- **问题**：资源调度成功后无法结束处置
- **修复**：
  - 新增 `POST /api/incident/complete` 端点
  - 前端新增"处置结束"按钮
  - 验证处置方案已接受，同时设置 `status = 'completed'`

### 14.5 角色权限完善

- **问题**：ADMIN无法驳回方案和结束处置
- **修复**：
  - `canRejectPlan` 增加 ADMIN 角色
  - `canCompleteIncident` 增加 ADMIN 和 RESOURCE_MANAGER

### 14.6 地图标记无显示（BigDecimal映射修复）

- **问题**：前端地图上无任何灾情标记点位
- **根因分析**：
  1. `Incident` 实体中 `latitude`/`longitude` 字段使用 `Double` 类型映射 MySQL `DECIMAL(10,7)` 类型
  2. JPA/Hibernate 对 `Double` → `DECIMAL` 的映射存在精度转换问题，导致坐标值读取为 null
  3. `findActiveIncidents()` 只返回 `processing` 状态的灾情，而大部分历史数据是 `completed` 状态
  4. 前端 Mock 数据中大屏接口只返回 `processing` 状态灾情
- **修复方案**：
  1. 将 `Incident.java` 实体中 `Double latitude/longitude` 改为 `BigDecimal latitude/longitude`
  2. 地理编码服务 `AmapGeocodeService.java` 返回的 `double[]` 通过 `BigDecimal.valueOf()` 转换存储
  3. 新增 `findWithCoordinates()` 查询方法，返回所有有坐标的灾情（不限状态）
  4. 前端 Mock 数据更新：大屏接口返回所有状态灾情，补全 `location`、`disposalPlanStatus`、`resourceDispatchStatus`、`deathCount`、`propertyLoss` 字段
- **验证结果**：API 返回 55 条有有效坐标的灾情数据，前端地图正确渲染所有标记点位

---

## 15. 已知待优化项

| 编号 | 问题 | 建议 | 优先级 |
|------|------|------|--------|
| OPT-001 | 方案生成响应时间较长（25-120秒） | 优化审查逻辑、预加载Embedding | 中 |
| OPT-002 | 方案审查评分标准过严 | 评分≥7分直接通过 | 中 |
| OPT-003 | Embedding模型首次加载耗时 | 服务启动时预加载 | 低 |
| OPT-004 | 资源调度无单独指令生成 | 可选：调度成功后自动生成指令 | 低 |
| OPT-005 | 前端未实现角色申请页面 | 可选：补充角色申请功能 | 低 |

---

## 16. 版本信息

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| V1.0 | 2026-07-26 | 初始结项文档 |
| V2.0 | 2026-07-27 | 增加地理编码服务、地图态势展示、历史数据坐标补全功能，更新数据库设计（incidents表增加latitude/longitude字段），移除冗余表（incident_reports、dispatch_orders、data_sources），更新灾情状态（新增pending、confirmed） |
| V3.0 | 2026-07-27 | 地图全链路打通：修复BigDecimal映射问题导致坐标为null、更新实体类型、修复查询方法、完善Mock数据。更新项目状态、数据统计、新增14.6节记录地图修复详情 |

---

**文档结束**

*云南自然灾害应急协同决策平台 - 多智能体驱动的应急响应系统*