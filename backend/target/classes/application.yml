server:
  port: 8080

spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  datasource:
    mysql:
      jdbc-url: jdbc:mysql://127.0.0.1:3306/emergency_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8
      username: root
      password: Llabb888
      driver-class-name: com.mysql.cj.jdbc.Driver
    postgres:
      jdbc-url: jdbc:postgresql://127.0.0.1:5432/emergency_vector
      username: postgres
      password: ZAQ12wsx581!
      driver-class-name: org.postgresql.Driver

  jpa:
    mysql:
      hibernate:
        ddl-auto: update
      show-sql: false
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.MySQLDialect
    postgres:
      hibernate:
        ddl-auto: validate
      show-sql: false
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: emergency-platform-jwt-secret-key-2024-yunnan-disaster-response
  expiration: 86400000

redis:
  host: localhost
  port: 6379
  timeout: 6000ms
  lettuce:
    pool:
      max-active: 8
      max-idle: 8
      min-idle: 2
      max-wait: -1ms

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

app:
  upload:
    dir: ./uploads

minio:
  url: http://localhost:9000
  access-key: admin
  secret-key: ZAQ12wsx581!
  bucket: emergency-images
  secure: false